/**
 * @animations/nodes
 *
 * The procedural node graph — a directed graph where nodes emit geometry, drive
 * materials, run simulations, or transform data. This is the platform's plugin
 * surface: third parties ship nodes, and using the platform *is* wiring nodes.
 *
 * See docs/ROADMAP.md → Phase 4.
 */

export const VERSION = "0.0.0";
